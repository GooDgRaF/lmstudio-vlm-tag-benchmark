Give image IDs in English.

Answer format: one ID per line.
At least 3 IDs if the image has at least 3 obvious visible features.
At most 10 IDs.

First give the 3 most obvious IDs.
If those 3 IDs are enough to capture the main content of the image, stop.
If important visible objects are still missing, add up to 3 more IDs.
If important visible objects are still missing after that, add up to 4 more IDs.
Do not add IDs just to reach a number.

Choose only matching IDs from the list below.
Answer format: one ID per line.

Rules:
- IDs only, no image description;
- no square brackets;
- no Markdown;
- no numbering;
- no explanations;
- do not guess non-obvious details; if unsure, skip.
