Give image tags in English.

Answer format: one tag per line.
At least 3 tags if the image has at least 3 obvious visible features.
At most {max_tags} tags.

First give the {first_chunk} most obvious tags.
If those {first_chunk} tags are enough to capture the main content of the image, stop.
If important visible objects are still missing, add up to {second_chunk} more tags.
If important visible objects are still missing after that, add up to {third_chunk} more tags.
Do not add tags just to reach a number.

Rules:
- tags only, no image description;
- each tag is short: 1-3 words;
- no Markdown;
- no numbering;
- no explanations;
- do not guess non-obvious details; if unsure, skip.
